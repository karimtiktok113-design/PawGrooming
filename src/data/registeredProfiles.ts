import { ClientProfile } from '../types/auth';

/**
 * PAW GROOMING — CLIENT PROFILES REPOSITORY
 * All client profiles are stored and synchronized directly with the live Firebase Firestore database.
 * No hardcoded profiles are stored in the website codebase.
 */
export const DEFAULT_REGISTERED_PROFILES: ClientProfile[] = [];
